# 更新日志

## v1.1.0 —— 与 CosyVoice3-MNN 合体 + 长文本链路修复（2026-09-18）

这一版把两条线合成一个可运行的例子：**VoiceDesign（纯文字设计音色）** + **CosyVoice3（朗读合成）**，
并修掉了朗读链上「长文本只读开头一句」的问题。真机：Honor Magic8 Pro（BKQ-AN90 / SM8850 / Android 17 / arm64-v8a）。

---

### 一、速度：提升多少（实测，不是估算）

| 项目 | 修复前 | 修复后 | 变化 |
|---|---|---|---|
| 朗读 LLM 段（150→155 token） | 长文本必撞上限→重试→CPU 兜底，整链几分钟 | **3.05 s / 67.2 tok/s** | 首次尝试即接受 |
| 朗读 LLM 段（150→151 token） | 同上 | **3.32 s / 58.7 tok/s** | 首次尝试即接受 |
| 朗读 LLM 段（231→452 token） | 5 次 NPU（每次 13.5~33.5 s）+ 2 次 CPU（87 s）**全部被拒** | **11.94 s / 47.6 tok/s** | 首次尝试即接受 |
| 长句 prefill（NPU） | 走 OpenCL：10.8~22.9 s | **0.62~0.78 s** | **快 17~35 倍** |
| 音色创建闭环（含设计+注册） | UI 里写的是「约 4~9 分钟」 | **97.3 s**（prefill 19.4 / decode 61.5 / decoder 6.6 / enroll 6.4） | 约 2.5~5.6 倍 |
| 注册参考音频 | 26 字 → 设计出 7.68 s → 被 5 s 上限截断 | 15 字 → **4.08 s / 102 token，不被截断** | 消除截断 |

> 口径说明：`音色创建 97.3 s` 是设备上测得的闭环总耗时（含设计模型解码 + enrollment）；
> 「4~9 分钟」是 App 内原本写给用户的提示文案。我没有留下修复前同口径的 design 墙钟记录，
> 所以这个倍数按「旧提示 vs 新实测」标注，不作为严格的前后对比。

---

### 二、遇到的问题 → 怎么定位 → 怎么解决

#### 问题 1：长文本只读开头一句（最关键）

**现象**：填 92 字（同一句重复 3 遍），只听到第一句；生成 token 数固定在 500（上限），没有 EOS。

**定位**：读构建机上的 MNN fork 源码（`transformers/llm/engine/src/sampler.cpp`）发现，
`cosyvoice_ras` 采样器的管线是**写死**的 `stepCosyVoiceRange → stepTopK → stepTopP → stepSelect`，
**不含 `penalty` 步**；而 `repetition_penalty` 只在 `penalty` 步里生效，MNN 默认的 `mixed_samplers`
也不含它。**也就是说这套 LLM 从来没有过重复惩罚** —— 模型一旦开始重复就再也出不来。

**解决**：runtime config 默认改为 `sampler_type=mixed` + `mixed_samplers=["penalty","topK","topP","temperature"]`
+ `repetition_penalty=1.15`。

**验证**（同一句 92 字，hexagon 后端）：

| 采样器 | 尝试 | 生成 token | EOS | unique | 最长连续重复 | 结果 |
|---|---:|---:|---|---:|---|---|
| `cosyvoice_ras`（旧） | 6 | 500 / 952 | ✗ 全部 | 57~219 | 33 / 314 / 345 / 610 / 858 | 全部被质量门拒绝 |
| `mixed` + `penalty` + 1.15 | **1** | **590** | **✓** | — | — | **接受，成品 23.56 s** |

#### 问题 2：`maxTokens` 写死 500 ≈ 只够 20 秒语音

**现象**：稍长的文本必然撞满 500 且没有 EOS。

**定位**：这条链的 LM 生成的是「音色前缀文本 + 正文」**整段** —— 音色档案里那 25 个字的参考文本
同样占 token。实测约 6 token/字：39 字 → 184 token；63 字 → 353~443；92 字 → 必然撞满 500。

**解决**：`maxTokens = (前缀字数 + 正文字数) * 8 + 128`，钳在 `[500, 1024]`（92 字实测取 952）。

#### 问题 3：质量门判据把「正常输出」也拒了

**现象**：修好采样器之前，同一句 63 字连续 7 次尝试全部被拒、整条链直接报错。

**定位**：旧判据 `longestRun >= 8 即拒` 在长输出上误杀 —— 那 7 次里有 4 次是**正常终止**
（370~387 token、有 EOS、unique 230~246），最长连续段只有 8~20 个 token（约 0.3~0.8 秒），
在语音 token 里对应长元音 / 静音 / 拖音，属正常范围；真正跑飞的那几次**都没有 EOS**，
且最长段 105~858。

**解决**：新判据只保留三条能证明退化的条件 ——

```text
1. 没有 EOS                生成只可能在 EOS 或 maxTokens 处停下，没 EOS 就是撞满上限
2. unique <= 4 且长度 >= 20  灾难性坍缩
3. 单段连续重复 >= 64        "正常"最大 20、"跑飞"最小 105，64 落在两者之间
```

并新增**尾部空转裁剪**：没有 EOS、重复段一直延伸到序列末尾、重复段 ≥32 且保留段 ≥32 时，
切掉重复段并重写 `speech-tokens-0.csv`（同时写 `llm-tail-repair.txt` 留证，不静默处理）。

#### 问题 4：「长句不能用 NPU」这个结论本身是错的

**现象**：原来按明文长度分流（≤24 字走 NPU，否则走 OpenCL），理由是「NPU 长序列约 3/4 退化」。

**定位（推翻自己的旧结论）**：

- 判据字段站不住：报告里的 `continuousHexagon` 算的是 `(配置名含 hexagon) && (hexagon-stage-layers.txt 非空)`，
  `npuFirstTokenValid` 直接等于 `hybridNpuPrefill`（= 配置含 hexagon 且 stage 文件为空）—— **两者都是配置回显，
  不是运行期测量**。而 stage 文件恰好由 App 自己在发起调用前写入，所以它必然为真。
- 阈值站不住：就是问题 3 里那个 `longestRun >= 8`。

两条腿都拆掉后实测同一句 63 字：**hexagon 4/4 首次尝试即通过**，prefill 0.62~0.73 s（OpenCL 要 10.8~22.9 s），
于是取消分流、一律用硬件策略给出的后端。

#### 问题 5：最后一次尝试不过质量门，退化音频被悄悄交付

**定位**：原来只有第一次尝试过门，最后那次 CPU 兜底跑完直接交付 —— 而 CPU 恰是退化率最高的那条路。

**解决**：尝试计划改为「路由后端 5 次 → CPU 兜底 2 次」，**每一次都过同一道门**，全不过就报错、不再交付。

---

### 三、怎么做到的（一句话版）

1. **拿到真源码**：构建机上 MNN fork 与 JNI 源码通过 `\\wsl$\Ubuntu\home\vicentrent\work\cosyvoice-npu\` 直读，
   把 LLM 引擎与 Hexagon/QNN 后端的 diff 固化成 `mnn-patches/` 下的 patch；
   `mnn-jni/CosyVoiceLlmPersistentBenchmark.cpp` 从 327 行的过期副本换成 465 行真源码。**没有源码就只能猜。**
2. **把状态量与实际测量分开**：凡是"回显配置"的字段（`continuousHexagon` / `npuFirstTokenValid`）都不再当证据。
3. **判据从「一次观测」改成「可复现的形态」**：没有 EOS / unique≤4 / 单段≥64 —— 每条都能在原始 token 上复算。
4. **每次改动都留向量**：6 个真机 token dump 进了单测（`CosyVoiceLlmAttemptPlanTest`，14 个用例）。

---

### 四、已知问题（本版未解决）

- **参考文本会被一并念出**：LM 生成的是「参考文本 + 正文」整段，App 把整段都当目标 token 交给 flow。
  要只读出正文，需要先确认 flow/conditioner 对「token 序列是否含提示区」的约定，不能凭猜改。
- **`decodeMs` 与 `wallMs` 出现过互相矛盾的读数**（809 s vs 639 s），该字段的差分在某些路径上不可靠，
  诊断时改用 `wallMs` 或产物文件时间。
- **`len266@[234-499]` 的重复起点**只有单次观测，不足以支撑结论。
